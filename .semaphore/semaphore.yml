version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      secrets:
        - name: test-secret
      jobs:
      - name: Build with secrets
        commands:
          - checkout
          - ls -lah
          - echo "keys"
          - echo $AWS_KEY
          - echo $GCLOUD_KEY
          - echo "files"
          - cat secret
          - cat sec1/big_file
          - echo "done"
